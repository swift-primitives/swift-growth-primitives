# swift-growth-primitives

Capacity-growth strategies — top-level `Growth.Policy<Element>`.
Hoisted from `Buffer.Growth` in the MSB tower W3 re-layering (tier-neutral
concern; the Buffer-namespaced form forced an upward Memory→Buffer dep and a
phantom substrate parameter under the `Buffer<S>` reparameterization).
